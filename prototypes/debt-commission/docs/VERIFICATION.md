# 第一場演出驗證

引擎：`4.7.stable.official.5b4e0cb0f`。使用同版本官方單執行緒 Web 範本、Compatibility renderer。驗證的是完整第一場《請幫我向你老闆討債》。

| 檢查 | 結果與證據 |
| --- | --- |
| 核准稿一致性 | 51 句正式台詞／心聲逐句比對一致；兩個玩家選項與回扣保留 |
| 劇情執行器 | `tests/test_story.gd` 通過：兩路結尾、旗標、分支回扣、無效選項、版本／節點還原檢查、重置、錯誤角色／站位／鏡頭引用 |
| 舞台 | `tests/test_stage.gd` 通過：登勢入場、新八移位、說話聚焦、表情、鏡頭、二進位序列化還原、取消 tween 後等待者可返回 |
| Web 匯出 | 已產出 `build/web/index.html` 及完整依賴，瀏覽器實際載入成功 |
| 桌機 A 路線 | Chromium，1280×900；55 個閱讀點，走到 `debt_recall_hearing` 後抵達結尾 |
| 觸控 B 路線 | Chromium 觸控模擬，390×844；54 個閱讀點，走到 `debt_recall_ledger` 後抵達結尾 |
| 窄畫面 | 320×568，兩個選項完整位於畫面內；恢復尺寸後仍可操作 |
| 續讀 | 兩路都在新八繞桌後重新整理、按繼續：同一句、同一分支旗標、三角色位置與可見性一致 |
| 閱讀與回顧 | 首次點擊補完文字；選項等待玩家；回顧開關不誤選；重玩清除問話方式與還款承諾 |
| 直接點擊對話框 | 觸控補完／下一句分別只執行一次，音效開關與回顧不推進劇情 |
| 一般試玩網址 | 不帶 `?qa=1` 時沒有 `window.__debtQA`；QA 只供測試觀察 |
| 錯誤紀錄 | 完整 A/B 與最後觸控檢查均無瀏覽器或 Godot 執行期錯誤 |

瀏覽器檢查使用真實 canvas 點擊及 touchscreen tap，沒有改寫遊戲狀態或跳關。A、B 結尾均保留各自的 `question_method`，且 `repayment_promised=true`，表示答應還款行動。

重跑方式見 [README](../README.md#驗證)。原始報告：[完整路線](browser-report.json)、[最後觸控檢查](smoke-report.json)。[手機選項畫面](preview-phone.png) 為實際 Web 截圖。

整合時已修正房間背景層級、桌面遮住角色臉部、選項與工具按鈕的焦點文字對比、續讀後 QA 狀態更新，以及觸控和模擬滑鼠造成的對話重複推進。

匯出檔案約 48.3 MiB，包含引擎 WASM、中文字型與故事資源，尚未做網路傳輸壓縮。受限環境中的 Godot 編輯器偵錯 socket 會回報無法 listen；匯出返回成功，實際 Web 執行與上述檢查通過。

尚待真人驗證：Android／iOS 實機、實際聲音感受、角色語氣、笑點和閱讀節奏。這些結果不由桌機觸控模擬或自動測試推定。
